<?php

namespace Admin\Policies;

use Admin\Http\Sections\PaymentSystems;
use App\User;
use App\PaymentSystem;
use Illuminate\Auth\Access\HandlesAuthorization;

class PaymentSystemsSectionModelPolicy
{

    use HandlesAuthorization;

    /**
     * @param User $user
     * @param string $ability
     * @param Roles $section
     * @param Role $item
     *
     * @return bool
     */
    public function before(User $user, $ability, PaymentSystems $section, PaymentSystem $item)
    {
        if ($user->isSuperAdmin()) {
//            if ($ability != 'display' && $ability != 'create' && $item->id <= 2) {
//
//            }

            return true;
        }
        return false;
    }

    /**
     * @param User $user
     * @param Roles $section
     * @param Role $item
     *
     * @return bool
     */
    public function display(User $user, PaymentSystems $section, PaymentSystem $item)
    {
        return $user->isSuperAdmin();
    }

    /**
     * @param User $user
     * @param Roles $section
     * @param Role $item
     *
     * @return bool
     */
    public function edit(User $user, PaymentSystems $section, PaymentSystem $item)
    {
        return $item->id > 2;
    }
    /**
     * @param User $user
     * @param Roles $section
     * @param Role $item
     *
     * @return bool
     */
    public function create(User $user, PaymentSystems $section, PaymentSystem $item)
    {
        return $item->id > 2;
    }
    /**
     * @param User $user
     * @param Roles $section
     * @param Role $item
     *
     * @return bool
     */
    public function delete(User $user, PaymentSystems $section, PaymentSystem $item)
    {
        return true; // $item->id > 2;
    }
}
